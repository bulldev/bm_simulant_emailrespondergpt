document.addEventListener('DOMContentLoaded', function() {
    // Initialize charts using the data
    initializeDailyStatsChart();
    initializeLanguageChart();
});

function initializeDailyStatsChart() {
    // Implementation for daily stats chart
}

function initializeLanguageChart() {
    // Implementation for language distribution chart
}